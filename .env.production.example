# Frontend Production Environment Template
# Copy to your hosting provider environment variables (do not commit real values)

# API
VITE_API_URL=https://api.your-domain.tld

# Supabase
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=replace-with-prod-anon-key

# Sentry (optional but recommended)
VITE_SENTRY_DSN=https://xxx@sentry.io/xxx
VITE_SENTRY_ENVIRONMENT=production
VITE_SENTRY_TRACES_SAMPLE_RATE=0.1
# Suggested: use CI to set this from the current git sha
VITE_SENTRY_RELEASE=agentsflowai-frontend@${GIT_SHA}
